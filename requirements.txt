# Core ML/Graph dependencies
# IMPORTANT: Install torch FIRST from pytorch.org, THEN install dgl.
# See README.md for step-by-step instructions.
#
# Do NOT pip install from this file directly — use the install steps in README.
# This file documents the required packages.

# PyTorch (install via --index-url https://download.pytorch.org/whl/cpu)
# torch==2.3.0
# torchvision==0.18.0
# torchaudio==2.3.0

# DGL (install via -f https://data.dgl.ai/wheels/torch-2.3/repo.html)
# dgl==2.2.1

# TxGNN (install from GitHub: pip install git+https://github.com/mims-harvard/TxGNN.git)
# TxGNN==0.0.3

# NLP / LLM
groq
python-dotenv

# Web UI
streamlit

# Evaluation & Data
scikit-learn
matplotlib
pandas
numpy<2
tqdm
requests
pyyaml
torchdata==0.7.1

# DGEM (Drug-Gene Expression Matching) module
scipy
GEOparse
